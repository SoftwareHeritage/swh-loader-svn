#!/usr/bin/make -f

export PYBUILD_NAME=swh.loader.svn
export PYBUILD_TEST_ARGS=-v -m "not db and not fs"

%:
	dh $@ --with python3 --buildsystem=pybuild

override_dh_install:
	dh_install
	rm -fv $(CURDIR)/debian/python3-*/usr/lib/python*/dist-packages/swh/__init__.py
	rm -fv $(CURDIR)/debian/python3-*/usr/lib/python*/dist-packages/swh/loader/__init__.py
